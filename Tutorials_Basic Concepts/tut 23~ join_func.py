'''
1. All the elements of a list,tuple can be joined together with the use of a "join" function

2. Syntax of "join" function is "string".join(name_of_list).The list/tuple should contain only strings and not any other data types.

3. In the example below space is repeated after every iteration till it reaches the last element of the list. Second
    example has a string instead of space which is getting iterated.
'''

# my_list =['shagun','singh','gunjan']
# a= ' '.join(my_list)
# print(a)

# my_list =['shagun','singh','gunjan']
# a= 'They are brother and sister'.join(my_list)
# print(a)